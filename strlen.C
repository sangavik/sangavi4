#include<stdio.h>
#include<string.h>
int main()
{
char text[400];
int length;
printf("\n enter the string ");
scanf("%s",&length);
len=str len(text);
printf("\n the length of string is :"len);
return 0;
}
